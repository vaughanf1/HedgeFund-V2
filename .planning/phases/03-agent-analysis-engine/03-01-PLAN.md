---
phase: 03-agent-analysis-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/agents/schemas.py
  - backend/app/agents/graph.py
  - backend/app/llm/wrapper.py
  - backend/app/events/__init__.py
  - backend/app/events/publisher.py
autonomous: true

must_haves:
  truths:
    - "AgentVerdict Pydantic schema matches the output format in persona Markdown templates exactly (persona, verdict, confidence, rationale, key_metrics_used, risks, upside_scenario, time_horizon, data_gaps)"
    - "LangGraph single-node StateGraph compiles and can be invoked via asyncio.run() from synchronous Celery-like context without RuntimeError"
    - "llm_call_with_persona_parsed returns a validated AgentVerdict Pydantic object via client.messages.parse()"
    - "publish_event publishes JSON to Redis Pub/Sub channel pipeline:events"
    - "anthropic SDK upgraded to >=0.86.0 and langgraph + langchain-anthropic added to requirements.txt"
  artifacts:
    - path: "backend/app/agents/schemas.py"
      provides: "AgentVerdict, CommitteeReport, CIODecision Pydantic models"
      exports: ["AgentVerdict", "CommitteeReport", "CIODecision"]
    - path: "backend/app/agents/graph.py"
      provides: "Per-persona LangGraph StateGraph with single run_persona node"
      exports: ["PERSONA_GRAPH", "build_persona_graph", "AgentState"]
    - path: "backend/app/llm/wrapper.py"
      provides: "llm_call_with_persona_parsed using messages.parse()"
      exports: ["llm_call_with_persona_parsed"]
    - path: "backend/app/events/publisher.py"
      provides: "Redis Pub/Sub event publishing helper"
      exports: ["publish_event", "EVENTS_CHANNEL"]
  key_links:
    - from: "backend/app/agents/graph.py"
      to: "backend/app/llm/wrapper.py"
      via: "import llm_call_with_persona_parsed"
      pattern: "llm_call_with_persona_parsed"
    - from: "backend/app/agents/graph.py"
      to: "backend/app/agents/schemas.py"
      via: "import AgentVerdict"
      pattern: "AgentVerdict"
---

<objective>
Build the foundational pieces for the agent analysis pipeline: AgentVerdict schema, LangGraph per-persona graph, structured output LLM wrapper, and Redis event publisher. This is the spike plan that validates LangGraph + asyncio.run() works from synchronous context and that messages.parse() returns a validated Pydantic object.

Purpose: Establishes all core building blocks that Plans 03-02 through 03-04 depend on. Validates the LangGraph + Celery asyncio bridge pattern before the full five-agent fan-out build. Every subsequent plan in this phase imports from files created here.

Output: Pydantic schemas (AgentVerdict, CommitteeReport, CIODecision), compiled LangGraph graph, extended LLM wrapper with structured output, Redis event publisher, updated requirements.txt.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-agent-analysis-engine/03-RESEARCH.md
@backend/app/llm/wrapper.py
@backend/app/agents/partitioner.py
@backend/app/agents/loader.py
@backend/app/agents/personas/buffett.md
@backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pydantic schemas + LangGraph graph + requirements update</name>
  <files>backend/requirements.txt, backend/app/agents/schemas.py, backend/app/agents/graph.py</files>
  <action>
**requirements.txt:**
- Upgrade `anthropic==0.40.0` to `anthropic>=0.86.0`.
- Add `langgraph==1.1.3`.
- Add `langchain-anthropic`.
- Add `sse-starlette`.
- Keep all other entries unchanged.

**schemas.py:**
- `from __future__ import annotations`
- Import BaseModel, Field from pydantic; Literal from typing.
- Define `AgentVerdict(BaseModel)` with fields matching persona template output format EXACTLY:
  - `persona: str`
  - `verdict: Literal["BUY", "HOLD", "PASS"]`
  - `confidence: int = Field(ge=0, le=100)`
  - `rationale: str`
  - `key_metrics_used: list[str]`
  - `risks: list[str]`
  - `upside_scenario: str`
  - `time_horizon: str`
  - `data_gaps: list[str]`
- Define `CommitteeReport(BaseModel)`:
  - `opportunity_id: str`
  - `verdicts: list[AgentVerdict]`
  - `consensus: Literal["BUY", "HOLD", "PASS", "SPLIT"]`
  - `dissent_agents: list[str]`
  - `variance_score: float`
  - `weighted_conviction: float`
  - `asymmetric_flag: bool`
  - `asymmetric_justification: str | None = None`
- Define `CIODecision(BaseModel)`:
  - `opportunity_id: str`
  - `conviction_score: int = Field(ge=0, le=100)`
  - `suggested_allocation_pct: float = Field(ge=0.0, le=100.0)`
  - `time_horizon: str`
  - `risk_rating: Literal["LOW", "MEDIUM", "HIGH", "VERY_HIGH"]`
  - `key_catalysts: list[str]`
  - `kill_conditions: list[str]`
  - `final_verdict: Literal["INVEST", "MONITOR", "PASS"]`

**graph.py:**
- `from __future__ import annotations`
- Import TypedDict, Any from typing; StateGraph, START, END from langgraph.graph; AgentVerdict from app.agents.schemas.
- Define `AgentState(TypedDict)`:
  - `persona_name: str`
  - `data_context: dict[str, Any]`
  - `redis_url: str`
  - `verdict: AgentVerdict | None`
- Define `async def run_persona_node(state: AgentState) -> dict[str, Any]`:
  - Import `llm_call_with_persona_parsed` from `app.llm.wrapper` (inside function to avoid circular import at module level).
  - Import `redis.asyncio as aioredis`.
  - Create async redis client from `state["redis_url"]`.
  - Call `verdict = await llm_call_with_persona_parsed(persona_name=state["persona_name"], data_context=state["data_context"], redis_client=redis_client)`.
  - Close redis client.
  - Return `{"verdict": verdict}`.
- Define `def build_persona_graph()`:
  - Create `StateGraph(AgentState)`.
  - Add node "run_persona" with `run_persona_node`.
  - Add edge START -> "run_persona".
  - Add edge "run_persona" -> END.
  - Return `builder.compile()`.
- Compile at module level: `PERSONA_GRAPH = build_persona_graph()`.
- Add docstring: "Single-node LangGraph graph for per-persona agent invocation. Compiled once at import time. Invoked via asyncio.run(PERSONA_GRAPH.ainvoke(state)) from synchronous Celery tasks."
  </action>
  <verify>
`cd /Users/vaughanfawcett/HedgeFund\ V2/backend && python -c "from app.agents.schemas import AgentVerdict, CommitteeReport, CIODecision; v = AgentVerdict(persona='buffett', verdict='BUY', confidence=75, rationale='test', key_metrics_used=['pe'], risks=['none'], upside_scenario='test', time_horizon='5y', data_gaps=[]); print(v.model_dump())"` — prints valid dict.
`grep 'langgraph' requirements.txt` — found.
`grep 'anthropic>=0.86' requirements.txt` — found.
  </verify>
  <done>AgentVerdict schema exactly matches persona template JSON output format. CommitteeReport and CIODecision schemas defined. LangGraph graph compiles with single run_persona node. Requirements updated with langgraph, langchain-anthropic, sse-starlette, and anthropic>=0.86.0.</done>
</task>

<task type="auto">
  <name>Task 2: Structured output LLM wrapper + Redis event publisher</name>
  <files>backend/app/llm/wrapper.py, backend/app/events/__init__.py, backend/app/events/publisher.py</files>
  <action>
**Extend wrapper.py — add `llm_call_with_persona_parsed` function:**
- Add import: `from app.agents.schemas import AgentVerdict` (at top, alongside existing imports).
- Add new async function `llm_call_with_persona_parsed` below existing `llm_call_with_persona`:
  - Signature: `async def llm_call_with_persona_parsed(persona_name: str, data_context: dict[str, Any], redis_client: Any, model: str = DEFAULT_MODEL, daily_limit_usd: float = DEFAULT_DAILY_LIMIT_USD, max_tokens: int = 1024) -> AgentVerdict:`
  - Create SpendTracker, do pre-flight budget check (same pattern as `llm_call`).
  - Create DataPartitioner and PersonaLoader instances.
  - Call `partitioned = partitioner.partition_raw(persona_name, data_context)`.
  - Call `system_prompt = loader.render_persona(persona_name, partitioned)`.
  - Create `client = anthropic.AsyncAnthropic()`.
  - Call `response = await client.messages.parse(model=model, max_tokens=max_tokens, system=system_prompt, messages=[{"role": "user", "content": "Analyse the data in your system prompt and return your verdict."}], output_format=AgentVerdict)`.
  - Wrap in try/except for `anthropic.APIError` and generic Exception, raising `LLMCallError` (same as `llm_call`).
  - Extract usage tokens and calculate cost via `calculate_call_cost`.
  - Record spend via `tracker.async_record_spend(cost_usd)`.
  - Log call completion with model, tokens, cost.
  - Return `response.parsed_output` — this is the validated `AgentVerdict` instance.
- Docstring: "Like llm_call_with_persona but uses messages.parse() for structured output. Returns a validated AgentVerdict Pydantic object instead of raw text."

**Create events package:**
- `backend/app/events/__init__.py` — empty.

**publisher.py:**
- `from __future__ import annotations`
- Import json, logging, redis (sync).
- `EVENTS_CHANNEL = "pipeline:events"`
- `logger = logging.getLogger(__name__)`
- Define `def publish_event(r: redis.Redis, event_type: str, payload: dict) -> int:`:
  - Build message: `json.dumps({"event": event_type, "data": payload}, default=str)`.
  - `num_subscribers = r.publish(EVENTS_CHANNEL, message)`.
  - `logger.info("Published %s event to %d subscribers", event_type, num_subscribers)`.
  - Return `num_subscribers`.
- Docstring at module level: "Redis Pub/Sub event publisher for pipeline state transitions. Events: AGENT_STARTED, AGENT_COMPLETE, COMMITTEE_COMPLETE, DECISION_MADE."

Note: publisher.py uses sync redis (not async) because it is called from Celery tasks which are synchronous. The SSE consumer endpoint (Plan 03-04) will use redis.asyncio to subscribe.
  </action>
  <verify>
`cd /Users/vaughanfawcett/HedgeFund\ V2/backend && python -c "from app.llm.wrapper import llm_call_with_persona_parsed; print('ok')"` — imports cleanly.
`cd /Users/vaughanfawcett/HedgeFund\ V2/backend && python -c "from app.events.publisher import publish_event, EVENTS_CHANNEL; print(EVENTS_CHANNEL)"` — prints "pipeline:events".
`grep 'messages.parse' backend/app/llm/wrapper.py` — found.
`grep 'output_format=AgentVerdict' backend/app/llm/wrapper.py` — found.
  </verify>
  <done>llm_call_with_persona_parsed uses messages.parse() with AgentVerdict schema and returns validated Pydantic object. Budget gate enforced before call. Redis event publisher publishes JSON to pipeline:events channel. Events package created.</done>
</task>

</tasks>

<verification>
1. `AgentVerdict` fields match persona template output format: persona, verdict, confidence, rationale, key_metrics_used, risks, upside_scenario, time_horizon, data_gaps
2. `CommitteeReport` and `CIODecision` schemas importable from `app.agents.schemas`
3. LangGraph `PERSONA_GRAPH` compiles at import time with START -> run_persona -> END
4. `llm_call_with_persona_parsed` calls `messages.parse()` with `output_format=AgentVerdict`
5. `publish_event` publishes to `pipeline:events` Redis Pub/Sub channel
6. `anthropic>=0.86.0` in requirements.txt
7. `langgraph==1.1.3`, `langchain-anthropic`, `sse-starlette` in requirements.txt
8. All new modules use `from __future__ import annotations`
</verification>

<success_criteria>
- All building blocks for Phase 3 agents are importable and tested
- LangGraph graph compiles without error
- Structured output wrapper extends existing llm_call pattern consistently
- Event publisher ready for use by fan-out tasks in Plan 03-02
- No circular imports introduced
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-analysis-engine/03-01-SUMMARY.md`
</output>
