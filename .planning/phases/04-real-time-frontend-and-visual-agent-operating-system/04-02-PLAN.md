---
phase: 04-real-time-frontend-and-visual-agent-operating-system
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/components/graph/edges/AnimatedFlowEdge.tsx
  - frontend/src/components/graph/nodes/AgentNode.tsx
  - frontend/src/components/graph/nodes/GateNode.tsx
  - frontend/src/components/graph/nodes/StageNode.tsx
  - frontend/src/components/graph/PipelineGraph.tsx
  - frontend/src/store/pipelineStore.ts
autonomous: true

must_haves:
  truths:
    - "When an AGENT_STARTED SSE event arrives, the corresponding agent node visually transitions to a running state with pulse animation and blue border"
    - "When an AGENT_COMPLETE SSE event arrives, the node transitions to complete state with emerald border and shows a brief result summary"
    - "Active edges (source node running) show an animated dot traveling along the edge path indicating data flow direction"
    - "Pipeline state transitions are visually clear: idle (zinc) -> running (blue pulse) -> complete (emerald) -> error (red)"
    - "Edge animations only appear on edges where the source node is in running state — not on all edges simultaneously"
  artifacts:
    - path: "frontend/src/components/graph/edges/AnimatedFlowEdge.tsx"
      provides: "SVG animateMotion edge with conditional active state"
      exports: ["AnimatedFlowEdge"]
    - path: "frontend/src/components/graph/nodes/AgentNode.tsx"
      provides: "Agent node with status-driven visual transitions"
  key_links:
    - from: "frontend/src/store/pipelineStore.ts"
      to: "frontend/src/components/graph/nodes/AgentNode.tsx"
      via: "node data.status drives visual state"
      pattern: "data\\.status"
    - from: "frontend/src/store/pipelineStore.ts"
      to: "frontend/src/components/graph/edges/AnimatedFlowEdge.tsx"
      via: "edge data.active derived from source node status"
      pattern: "data\\.active"
---

<objective>
Add animated state transitions to pipeline graph nodes and animated flow edges — making the pipeline visually alive when SSE events arrive.

Purpose: The static graph from 04-01 needs to respond to real-time events. This plan wires node visual state changes (idle/running/complete/error) with smooth transitions and adds animated dot-traveling edges to show data flow direction through the pipeline.

Output: Pipeline graph nodes animate through states as SSE events arrive; edges show directional flow animation when active.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-frontend-and-visual-agent-operating-system/04-RESEARCH.md
@.planning/phases/04-real-time-frontend-and-visual-agent-operating-system/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animated flow edge component and edge state management</name>
  <files>
    frontend/src/components/graph/edges/AnimatedFlowEdge.tsx
    frontend/src/store/pipelineStore.ts
    frontend/src/components/graph/PipelineGraph.tsx
  </files>
  <action>
    1. **AnimatedFlowEdge** (`src/components/graph/edges/AnimatedFlowEdge.tsx`):
       - Import `BaseEdge, EdgeProps, getSmoothStepPath` from `@xyflow/react`
       - Renders a `BaseEdge` with stroke color `#3f3f46` (zinc-700) by default
       - When `data.active` is true, stroke changes to `#0A84FF` (primary blue)
       - When active, render an SVG `<circle r="4" fill="#0A84FF">` with `<animateMotion dur="1.5s" repeatCount="indefinite" path={edgePath} />`
       - Use `getSmoothStepPath` for edge routing (matches dagre layout better than bezier)

    2. **Update store** to compute edge `active` state:
       - In the `updateNodeStatus` action, after updating the target node, also update edges:
         - For each edge where `edge.source === nodeId` and the new status is `'running'`, set `edge.data = { ...edge.data, active: true }`
         - For each edge where `edge.source === nodeId` and the new status is NOT `'running'`, set `edge.data = { ...edge.data, active: false }`
       - This means: when "quality_gate" transitions to "running", the 5 edges from quality_gate to agents become active. When it completes, they deactivate.

    3. **Register edgeTypes** in PipelineGraph.tsx:
       - Define `const edgeTypes = { animated: AnimatedFlowEdge }` at module level
       - Update initial edges in the store to use `type: 'animated'` for all edges
       - Pass `edgeTypes` to `<ReactFlow>`
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - AnimatedFlowEdge component exports correctly
    - Edge type registered at module level (NOT inside component render)
  </verify>
  <done>Animated edge component renders with conditional dot animation; edges activate when source node is running</done>
</task>

<task type="auto">
  <name>Task 2: Enhanced node state transitions with visual polish</name>
  <files>
    frontend/src/components/graph/nodes/AgentNode.tsx
    frontend/src/components/graph/nodes/GateNode.tsx
    frontend/src/components/graph/nodes/StageNode.tsx
  </files>
  <action>
    1. **Enhance AgentNode** with richer state display:
       - Add a small colored status dot (4px circle) in the header next to the persona name:
         - idle: zinc-500, running: blue-500 with animate-pulse, complete: emerald-500, error: red-500
       - Add CSS transition on border-color and background-color: `transition-all duration-500 ease-in-out`
       - When status is `complete` and `data.lastResult` exists, show a truncated verdict line (e.g. "BUY | 78 conviction") in zinc-400 text below the name
       - When status is `running`, show a small "Analyzing..." text with animate-pulse
       - Add a subtle glow effect on running state: `shadow-[0_0_12px_rgba(10,132,255,0.3)]`

    2. **Enhance GateNode**:
       - Add a funnel/filter icon (use a simple SVG inline or unicode character like "⬡")
       - Status-driven: idle=zinc, running=yellow-500 border + "Filtering..." text, complete=emerald, error=red
       - When complete, show count: "X passed / Y total" if data includes pass counts
       - CSS transition on all color changes

    3. **Enhance StageNode**:
       - Scanner node: show last scan time from data if available
       - Committee node: when complete, show consensus info (e.g. "BUY consensus 72%")
       - CIO node: when complete, show final verdict + conviction
       - All stage nodes: same status dot pattern as AgentNode
       - CSS transitions on state changes

    4. **All nodes**: Ensure `memo()` wrapping is in place. Ensure Handles have `className="!bg-blue-500 !w-2 !h-2"` for styled connection points matching the Bloomberg theme.

    IMPORTANT: All three node components must remain defined at module level and exported. The `nodeTypes` object in PipelineGraph.tsx must reference these module-level exports. Do NOT move nodeTypes into a component body.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Each node component is wrapped with `memo()`
    - Status classes include `transition-all duration-500`
    - Running state includes animate-pulse on status dot
    - All Handle elements have styled classNames
  </verify>
  <done>All pipeline nodes show polished status-driven visual transitions with glow effects, status dots, contextual status text, and smooth CSS transitions</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. `npm run dev` — graph renders with enhanced node styling
3. Nodes show idle state (zinc borders/backgrounds) by default
4. If manually calling `store.updateNodeStatus('buffett', 'running')` in console, the Buffett node shows blue border + pulse + glow
5. Edges connected from a running node show the animated blue dot
6. State transitions have visible CSS transitions (not instant snapping)
</verification>

<success_criteria>
- AnimatedFlowEdge shows traveling dot on active edges only
- Agent nodes transition through 4 visual states with smooth CSS transitions
- Running nodes have blue glow + pulse animation
- Complete nodes show result summary text
- All node/edge types defined at module level
- No re-render issues from nodeTypes/edgeTypes recreation
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-frontend-and-visual-agent-operating-system/04-02-SUMMARY.md`
</output>
