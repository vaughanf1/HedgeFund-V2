---
phase: 04-real-time-frontend-and-visual-agent-operating-system
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/components/feed/OpportunityFeed.tsx
  - frontend/src/components/feed/FeedItem.tsx
  - frontend/src/components/inspect/OpportunitySheet.tsx
  - frontend/src/components/inspect/AgentVerdictPanel.tsx
  - frontend/src/store/pipelineStore.ts
  - frontend/src/types/pipeline.ts
autonomous: true

must_haves:
  truths:
    - "New opportunity detections appear in the live feed in real time as SSE DECISION_MADE events arrive"
    - "Feed shows three sections: trending/highest conviction, recent detections, and recently rejected with rejection reasons"
    - "Clicking a feed item opens a slide-over Sheet showing the full pipeline stage breakdown for that opportunity"
    - "The inspection Sheet shows all five agent verdicts with scores and reasoning"
    - "Feed panel does NOT re-render when graph nodes are dragged or panned (Zustand slice selector isolation)"
  artifacts:
    - path: "frontend/src/components/feed/OpportunityFeed.tsx"
      provides: "Live feed with trending, recent, and rejected sections"
    - path: "frontend/src/components/feed/FeedItem.tsx"
      provides: "Individual feed item card with ticker, conviction, timestamp"
    - path: "frontend/src/components/inspect/OpportunitySheet.tsx"
      provides: "shadcn Sheet slide-over for opportunity detail inspection"
    - path: "frontend/src/components/inspect/AgentVerdictPanel.tsx"
      provides: "Per-agent verdict display with score breakdown"
    - path: "frontend/src/types/pipeline.ts"
      provides: "TypeScript interfaces for FeedItem, Opportunity, AgentScore"
  key_links:
    - from: "frontend/src/components/feed/OpportunityFeed.tsx"
      to: "frontend/src/store/pipelineStore.ts"
      via: "usePipelineStore((s) => s.feedItems) slice selector"
      pattern: "usePipelineStore.*feedItems"
    - from: "frontend/src/components/inspect/OpportunitySheet.tsx"
      to: "/api/v1/opportunities/{id}"
      via: "fetch call for full detail"
      pattern: "fetch.*opportunities"
---

<objective>
Build the live opportunity feed panel and pipeline inspection views — the real-time stream of detections, trending ideas, and rejected plays, plus drill-down detail sheets.

Purpose: The feed panel (middle panel of the three-panel layout) shows what the system is discovering in real time. The inspection Sheet lets users drill into any opportunity to see the full agent analysis without leaving the dashboard. This delivers FEED-01, FEED-02, FEED-03, VIS-02, and VIS-03.

Output: Live feed updating in real time with categorized opportunity items; slide-over Sheet showing full per-agent breakdown on click.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-frontend-and-visual-agent-operating-system/04-RESEARCH.md
@.planning/phases/04-real-time-frontend-and-visual-agent-operating-system/04-01-SUMMARY.md
@backend/app/routers/opportunities.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript types, feed store updates, and FeedItem component</name>
  <files>
    frontend/src/types/pipeline.ts
    frontend/src/store/pipelineStore.ts
    frontend/src/components/feed/FeedItem.tsx
  </files>
  <action>
    1. **TypeScript interfaces** (`src/types/pipeline.ts`):
       - `FeedItem`: `{ id: string, type: 'detection' | 'decision' | 'rejection', ticker: string, headline: string, convictionScore?: number, riskRating?: string, finalVerdict?: string, rejectionReason?: string, timestamp: number }`
       - `Opportunity`: `{ opportunityId: string, ticker: string, convictionScore: number, riskRating: string, suggestedAllocationPct: number, finalVerdict: string, expectedUpside?: string, timeHorizon?: string, keyCatalysts?: string[], agentScores: AgentScore[], cioSummary?: string, decidedAt: string }`
       - `AgentScore`: `{ persona: string, verdict: string, confidence: number, rationale?: string, risks?: string[], upsideScenario?: string, timeHorizon?: string }`
       - `NodeStatus`: `'idle' | 'running' | 'complete' | 'error'`
       - `PipelineNodeData`: `{ label: string, status: NodeStatus, type: string, lastResult?: string, [key: string]: unknown }`

    2. **Update Zustand store** to use these types:
       - Import types from `src/types/pipeline.ts`
       - Refine `handleSSEEvent` to populate `feedItems` properly:
         - On `DECISION_MADE`: Parse the event data to extract ticker, conviction_score, final_verdict, risk_rating. Create a FeedItem with type='decision' if verdict is BUY/HOLD/MONITOR, or type='rejection' if verdict is PASS/SELL. Prepend to feedItems (max 100 items).
         - On `AGENT_COMPLETE`: Create a FeedItem with type='detection' showing "Agent {persona} completed analysis on {ticker}". Prepend to feedItems.
       - Add `selectedOpportunityId: string | null` state and `setSelectedOpportunity(id: string | null)` action for the inspection sheet

    3. **FeedItem component** (`src/components/feed/FeedItem.tsx`):
       - Card-style component using shadcn Card with compact padding
       - Shows: ticker (font-mono, bold, white), headline (zinc-400, text-xs), relative timestamp ("2m ago")
       - Left color bar indicator: blue for detection, emerald for approved decision, red for rejection
       - If conviction score present: show as small badge (e.g. "78" in blue)
       - If rejection: show reason in red-400 text-xs
       - onClick: calls `setSelectedOpportunity(item.id)` to open the inspection sheet
       - Use `memo()` to prevent unnecessary re-renders
  </action>
  <verify>
    - `npx tsc --noEmit` passes with new types
    - FeedItem renders with all visual variants (detection, decision, rejection)
    - Store correctly categorizes DECISION_MADE events as decision vs rejection based on verdict
  </verify>
  <done>TypeScript types defined, store updated with feed item generation from SSE events, FeedItem component renders with color-coded variants</done>
</task>

<task type="auto">
  <name>Task 2: OpportunityFeed panel with sections, OpportunitySheet inspection view</name>
  <files>
    frontend/src/components/feed/OpportunityFeed.tsx
    frontend/src/components/inspect/OpportunitySheet.tsx
    frontend/src/components/inspect/AgentVerdictPanel.tsx
  </files>
  <action>
    1. **OpportunityFeed** (`src/components/feed/OpportunityFeed.tsx`) — replaces placeholder:
       - Subscribe to `feedItems` via Zustand slice selector: `usePipelineStore((s) => s.feedItems)` — NOT the full store
       - Three sections with shadcn Separator between them:
         a. **Highest Conviction** (top): Filter feedItems for type='decision', sort by convictionScore desc, show top 3. Header: "Highest Conviction" with a flame icon or similar. If empty: "No approved opportunities yet"
         b. **Recent Activity** (middle): All feedItems in chronological order (most recent first), show up to 20. Header: "Live Feed". Use shadcn ScrollArea for overflow.
         c. **Recently Rejected** (bottom): Filter for type='rejection', show last 5. Header: "Rejected" in red-400. Each shows ticker + rejection reason.
       - Wrap in ScrollArea for the full panel
       - Panel header: "Opportunity Feed" in font-mono uppercase tracking-wider zinc-400, with a small green pulse dot indicating SSE connection

    2. **OpportunitySheet** (`src/components/inspect/OpportunitySheet.tsx`):
       - Uses shadcn `Sheet` component (slide-over from right side)
       - Open state driven by `selectedOpportunityId` from store (non-null = open)
       - On open, fetch full detail from `GET /api/v1/opportunities/{id}` using native `fetch`
       - Display loading spinner while fetching
       - Header: ticker in large font-mono bold + final verdict badge (BUY=emerald, SELL=red, HOLD=yellow, PASS=zinc, MONITOR=blue)
       - Body sections:
         a. **CIO Decision**: conviction score (large number), suggested allocation %, risk rating badge, time horizon, key catalysts as bullet list
         b. **Agent Breakdown**: Render AgentVerdictPanel for each of the 5 verdicts
         c. **Kill Conditions**: if present in CIO decision JSON, render as red-tinted list
       - On close: `setSelectedOpportunity(null)`
       - Handle 404 gracefully (opportunity not found)

    3. **AgentVerdictPanel** (`src/components/inspect/AgentVerdictPanel.tsx`):
       - Collapsible section (default expanded) for each agent persona
       - Header: persona name + verdict badge + confidence score bar (0-100, colored bar)
       - Expanded body: rationale text (zinc-300), risks as red-tinted bullets, upside scenario in emerald-tinted text, time horizon
       - Persona icon: first letter of persona name in a colored circle (each persona gets a distinct color: Buffett=amber, Munger=violet, Ackman=cyan, Cohen=pink, Dalio=sky)
       - Use shadcn Card for each panel with zinc-900 background

    IMPORTANT: The OpportunityFeed must use `usePipelineStore((s) => s.feedItems)` — NOT `usePipelineStore()` without a selector. Direct store access would cause re-renders on every node position change from the graph panel.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - OpportunityFeed renders three sections in the middle panel
    - Clicking a feed item opens the Sheet from the right side
    - Sheet displays loading state, then full opportunity breakdown
    - AgentVerdictPanel shows per-persona colored headers
    - Sheet closes cleanly when clicking outside or pressing Escape
  </verify>
  <done>Live feed shows categorized opportunities in real time, inspection Sheet shows full per-agent breakdown with CIO decision detail</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. `npm run dev` — middle panel shows "Opportunity Feed" with three sections
3. If an SSE DECISION_MADE event arrives, a new FeedItem appears at the top of "Live Feed"
4. Clicking a feed item opens a right-side Sheet with opportunity details
5. Sheet shows CIO decision + 5 agent verdict panels
6. Feed does NOT re-render when dragging nodes in the graph panel
7. Sheet closes on Escape or overlay click
</verification>

<success_criteria>
- Live feed categorizes items into highest conviction, recent activity, and rejected
- Feed updates in real time from SSE events without page refresh
- Inspection Sheet fetches and displays full opportunity detail from REST API
- Per-agent verdict panels show persona-colored headers with scores and reasoning
- Feed panel uses Zustand slice selector for render isolation from graph
- Sheet preserves dashboard context (graph and feed visible behind overlay)
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-frontend-and-visual-agent-operating-system/04-03-SUMMARY.md`
</output>
