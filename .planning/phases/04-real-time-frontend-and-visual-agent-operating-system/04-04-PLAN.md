---
phase: 04-real-time-frontend-and-visual-agent-operating-system
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/components/output/OutputDashboard.tsx
  - frontend/src/components/output/OpportunityCard.tsx
  - frontend/src/components/output/AgentBreakdown.tsx
  - frontend/src/store/pipelineStore.ts
  - frontend/src/hooks/useOpportunities.ts
autonomous: true

must_haves:
  truths:
    - "Right panel shows the top 5-10 ranked opportunities sorted by conviction score descending"
    - "Each opportunity card displays conviction score, risk rating, expected upside, time horizon, and key catalysts"
    - "Each opportunity card shows per-agent score breakdown with persona name and score"
    - "CIO summary with final recommendation is visible for each opportunity"
    - "Dashboard updates when new DECISION_MADE events arrive via SSE without page refresh"
  artifacts:
    - path: "frontend/src/components/output/OutputDashboard.tsx"
      provides: "Ranked opportunity list in right panel"
    - path: "frontend/src/components/output/OpportunityCard.tsx"
      provides: "Individual opportunity card with full breakdown"
      exports: ["OpportunityCard"]
    - path: "frontend/src/components/output/AgentBreakdown.tsx"
      provides: "Per-agent score row component"
      exports: ["AgentBreakdown"]
    - path: "frontend/src/hooks/useOpportunities.ts"
      provides: "Hook to fetch initial opportunities from REST API"
      exports: ["useOpportunities"]
  key_links:
    - from: "frontend/src/components/output/OutputDashboard.tsx"
      to: "frontend/src/store/pipelineStore.ts"
      via: "usePipelineStore((s) => s.outputRankings) slice selector"
      pattern: "usePipelineStore.*outputRankings"
    - from: "frontend/src/hooks/useOpportunities.ts"
      to: "/api/v1/opportunities"
      via: "fetch on mount"
      pattern: "fetch.*opportunities"
---

<objective>
Build the final output dashboard panel — the ranked list of top opportunities with full per-agent breakdowns and CIO recommendations.

Purpose: The output dashboard (right panel) is the primary decision surface. It shows the system's top investment recommendations ranked by conviction, with all the detail a user needs to evaluate each opportunity. This delivers OUT-01 through OUT-04.

Output: Right panel showing ranked opportunity cards with conviction scores, risk ratings, per-agent breakdowns, and CIO summaries — updating in real time.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-time-frontend-and-visual-agent-operating-system/04-RESEARCH.md
@.planning/phases/04-real-time-frontend-and-visual-agent-operating-system/04-01-SUMMARY.md
@backend/app/routers/opportunities.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: useOpportunities hook, store hydration, AgentBreakdown component</name>
  <files>
    frontend/src/hooks/useOpportunities.ts
    frontend/src/store/pipelineStore.ts
    frontend/src/components/output/AgentBreakdown.tsx
  </files>
  <action>
    1. **useOpportunities hook** (`src/hooks/useOpportunities.ts`):
       - On mount, fetch `GET /api/v1/opportunities?limit=10` to hydrate the output panel with existing data
       - For each summary returned, fetch `GET /api/v1/opportunities/{id}` to get full detail (agent verdicts + CIO decision)
       - Map the API response to the `Opportunity` TypeScript type (from `src/types/pipeline.ts`)
       - Call `store.setOutputRankings(opportunities)` to hydrate the store
       - Handle fetch errors gracefully (log warning, show empty state)
       - Only run once on mount — subsequent updates come from SSE events
       - Note: If the backend is not running, this fails silently and the panel shows "No opportunities yet"

    2. **Update store** `setOutputRankings` and `handleSSEEvent`:
       - `setOutputRankings(rankings: Opportunity[])`: Set outputRankings, sorted by convictionScore desc, capped at 10
       - Enhance `DECISION_MADE` handler: Parse the full event data into an Opportunity object and insert/update in outputRankings. If the opportunity already exists (same opportunityId), replace it. Sort by convictionScore desc. Cap at 10.
       - Add `fetchOpportunityDetail(id: string): Promise<Opportunity>` as a standalone async function (NOT a store action — Zustand actions should be sync). The hook calls this, then calls the store setter.

    3. **AgentBreakdown** (`src/components/output/AgentBreakdown.tsx`):
       - Compact row component for use inside OpportunityCard
       - Shows: persona name (with persona color dot matching 04-03 colors — Buffett=amber, Munger=violet, Ackman=cyan, Cohen=pink, Dalio=sky), verdict (BUY/SELL/HOLD badge), confidence score as a thin horizontal bar (0-100, colored by value: <40=red, 40-70=yellow, >70=emerald)
       - Hover tooltip (native title attr) showing the agent's one-line rationale if available
       - Font: text-xs, font-mono for scores
       - Memo-wrapped
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - useOpportunities hook fetches and hydrates store on mount
    - AgentBreakdown renders persona-colored rows with confidence bars
    - Store correctly inserts/replaces opportunities on DECISION_MADE events
  </verify>
  <done>REST API hydration hook working, store handles both initial load and SSE updates for output rankings, AgentBreakdown component renders per-persona score rows</done>
</task>

<task type="auto">
  <name>Task 2: OpportunityCard and OutputDashboard panel</name>
  <files>
    frontend/src/components/output/OpportunityCard.tsx
    frontend/src/components/output/OutputDashboard.tsx
    frontend/src/App.tsx
  </files>
  <action>
    1. **OpportunityCard** (`src/components/output/OpportunityCard.tsx`):
       - Uses shadcn Card with zinc-900 background and zinc-800 border
       - **Header row**: Ticker (font-mono, text-lg, font-bold, white) | Final verdict badge (BUY=emerald, SELL=red, HOLD=yellow, PASS=zinc, MONITOR=blue) | Risk rating badge (LOW=emerald, MEDIUM=yellow, HIGH=red, using the semi-transparent colored badge pattern from research)
       - **Conviction score**: Large number (text-3xl, font-bold, blue-400) with "conviction" label in zinc-400 text-sm. Show as percentage-style: "78" not "0.78".
       - **Key metrics row** (grid-cols-3, text-xs, zinc-400):
         - Suggested allocation: "{X}%"
         - Time horizon: from CIO decision (e.g. "3-6 months")
         - Expected upside: if available from CIO decision data
       - **Key catalysts**: If present, render as compact bullet list (text-xs, zinc-300) with a small star icon
       - **Agent breakdown section**: Header "Agent Analysis" in zinc-500 uppercase tracking-wider text-xs. Render AgentBreakdown for each of the 5 agents. If agentScores is empty, show "Awaiting agent analysis..."
       - **CIO summary**: If cioSummary exists, render in a zinc-800 rounded box with "CIO" label and summary text in zinc-300, text-xs
       - Subtle left border accent colored by verdict (emerald for BUY, red for SELL, etc.)
       - Memo-wrapped

    2. **OutputDashboard** (`src/components/output/OutputDashboard.tsx`) — replaces placeholder:
       - Subscribe to `outputRankings` via Zustand slice selector: `usePipelineStore((s) => s.outputRankings)`
       - Panel header: "Top Opportunities" in font-mono uppercase tracking-wider zinc-400, with count badge showing "{N} active"
       - Render OpportunityCard for each opportunity in outputRankings (already sorted by conviction)
       - Wrap in shadcn ScrollArea for overflow
       - Empty state: centered message "No opportunities analyzed yet" with a subtle icon, zinc-500 text
       - Add the `useOpportunities('/api/v1/opportunities')` hook call to hydrate on mount

    3. **Wire into App.tsx**:
       - Ensure `useOpportunities` is called at app level (or in OutputDashboard — either works since it hydrates the store)
       - The hook should be called exactly once, NOT in both places

    IMPORTANT: OutputDashboard must use `usePipelineStore((s) => s.outputRankings)` slice selector — NOT the full store. This prevents re-renders from graph node changes.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run dev` — right panel shows "Top Opportunities" header
    - If backend is running with data, opportunities load on mount and display as cards
    - Each card shows conviction score, risk rating, verdict badge, and agent breakdown rows
    - Panel does NOT re-render when dragging graph nodes
  </verify>
  <done>Output dashboard renders ranked opportunity cards with full breakdowns; hydrates from REST API on mount; updates in real time from SSE</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no TypeScript errors
2. `npm run dev` — right panel shows "Top Opportunities" with cards (or empty state if no data)
3. Each OpportunityCard shows: ticker, verdict badge, risk badge, conviction score, allocation %, time horizon, catalysts, 5 agent breakdown rows, CIO summary
4. Cards are sorted by conviction score descending
5. New DECISION_MADE events add/update cards in real time
6. Panel uses slice selector (verify no re-render on graph drag)
7. Empty state renders cleanly when no opportunities exist
</verification>

<success_criteria>
- Top 5-10 opportunities ranked by conviction score in the right panel
- Each card displays: conviction score, risk rating, allocation %, time horizon, key catalysts
- Per-agent score breakdown visible with persona-colored confidence bars
- CIO summary displayed per opportunity
- Dashboard hydrates from REST API on mount
- Real-time updates from SSE events
- Slice selector prevents cross-panel re-render cascades
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-time-frontend-and-visual-agent-operating-system/04-04-SUMMARY.md`
</output>
